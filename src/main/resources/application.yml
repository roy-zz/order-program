spring:
  application:
    name: order-program
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    jdbc-url: jdbc:h2:mem:order_program;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;AUTO_RECONNECT=TRUE;
    driver-class-name: org.h2.Driver
    username: sa
    password: password
